namespace P4ToCSharp.App

module JsonToGrammar =

  open P4Grammar
  open JsonTypes

  let grammarOfJson = ()

  (*
  
  We have a problem - we cannot really type the grammar datatypes unless they are annotated with node IDs.

  *)

